#include <bits/stdc++.h>
using namespace std;

int main() {
    
    vector<vector<string>> s;

    s.push_back({""});
    s.push_back({""});
    s.push_back({""});

    // cout << s[0][0] << endl;
    cout << s[0][0].size() << endl;

    return 0;
}